[build-dependencies]
protobuf-codegen = {version=3.2,features=[]} # libp2p_bitswap
serde = {features=['derive']} # rpc_api
syn = {version=2.0,features=[]} # rpc_api
walkdir = {version=2.3,features=[]} # libp2p_bitswap

[dependencies]
argon2 = {version=0.5,features=[]} # key_management
async-recursion = {version=1.0,features=[]} # ipld
axum = {features=['ws']} # rpc
backoff = {features=['tokio']} # utils
byte-unit = {features=[]} # cli_shared
cid = {features=['std'],default_features=False} # libp2p
const-str = {version=0.5.4,features=[]} # networks
crossbeam = {version=0.8,features=[]} # rpc
crossbeam-channel = {version=0.5,features=[]} # state_migration
data-encoding = {version=2.3,features=[]} # json
fvm_ipld_encoding3 = {version=0.3,package=fvm_ipld_encoding,features=[]} # shim
fvm_shared = {features=[],default_features=False} # blocks
fvm_shared = {features=[],default_features=False} # fil_cns
fvm_shared = {features=[],default_features=False} # interpreter
fvm_shared = {features=[],default_features=False} # ipld
fvm_shared = {features=[],default_features=False} # json
fvm_shared = {features=['testing'],default_features=False} # message
fvm_shared = {features=[],default_features=False} # message_pool
fvm_shared3 = {features=[],default_features=False} # chain
fvm_shared3 = {features=['proofs']} # interpreter
fvm_shared3 = {features=[],default_features=False} # json
fvm_shared3 = {features=['testing'],default_features=False} # message
fvm_shared3 = {features=[],default_features=False} # message_pool
fvm_shared3 = {features=[],default_features=False} # rpc
fvm_shared3 = {features=['proofs', 'arb']} # shim
jsonwebtoken = {version=8.1,features=[]} # auth
libipld = {features=['serde-codec'],default_features=False} # libp2p_bitswap
libipld-core = {features=['serde-codec']} # ipld
libp2p = {features=['gossipsub', 'kad', 'identify', 'ping', 'mdns', 'noise', 'yamux', 'tcp', 'websocket', 'dns', 'request-response', 'metrics', 'tokio', 'macros'],default_features=False} # libp2p
libp2p = {features=['request-response', 'macros'],default_features=False} # libp2p_bitswap
log = {features=['serde']} # cli_shared
memory-stats = {version=1.1,features=[]} # utils
mimalloc = {version=0.1.34,features=[],optional=True,default_features=False} # cli_shared
multihash = {features=['std', 'blake2b', 'derive', 'serde-codec', 'identity'],default_features=False} # json
multihash = {features=['std', 'multihash-impl', 'identity', 'sha2'],default_features=False} # libp2p
parity-db = {version=0.4.6,features=[],optional=True,default_features=False} # db
prometheus = {features=['process']} # chain_sync
prometheus = {features=['process']} # metrics
protobuf = {version=3.2,features=[]} # libp2p_bitswap
rand_distr = {version=0.4,features=[]} # rpc
rocksdb = {version=0.20,features=['lz4'],optional=True,default_features=False} # db
semver = {version=1.0,features=[]} # rpc
serde = {features=['derive'],default_features=False} # auth
serde = {features=['derive']} # beacon
serde = {features=['derive']} # blocks
serde = {features=['derive']} # chain
serde = {features=['derive', 'rc']} # chain_sync
serde = {features=['derive']} # cli_shared
serde = {features=['derive']} # db
serde = {features=['derive']} # ipld
serde = {features=['derive']} # json
serde = {features=['derive']} # key_management
serde = {features=['derive']} # libp2p
serde = {features=['derive']} # libp2p_bitswap
serde = {features=['derive']} # message
serde = {features=['derive']} # message_pool
serde = {features=['derive']} # networks
serde = {features=['derive'],default_features=False} # rpc
serde = {features=['derive'],default_features=False} # rpc_api
serde = {features=['derive']} # shim
serde = {features=['derive']} # state_manager
serde = {features=['derive']} # statediff
serde = {features=['derive']} # utils
sha2 = {features=[],default_features=False} # beacon
sha2 = {features=[],default_features=False} # rpc
similar = {version=2.2.1,features=[]} # statediff
slotmap = {version=1.0,features=[]} # message_pool
statrs = {version=0.16,features=[]} # message_pool
stdext = {version=0.3,features=[],optional=True} # interpreter
strum_macros = {version=0.24,features=[]} # networks
tikv-jemallocator = {version=0.5,features=[],optional=True} # cli_shared
tokio = {features=['sync', 'rt-multi-thread']} # beacon
tokio = {features=['sync']} # chain
tokio = {features=['sync']} # chain_sync
tokio = {features=['sync', 'process']} # cli_shared
tokio = {features=['sync']} # db
tokio = {features=['sync', 'time']} # deleg_cns
tokio = {features=['sync']} # fil_cns
tokio = {features=['sync', 'time']} # libp2p
tokio = {features=['rt-multi-thread', 'time', 'macros']} # libp2p_bitswap
tokio = {features=['sync', 'macros']} # message_pool
tokio = {features=['sync', 'fs']} # rpc
tokio = {features=['sync']} # rpc_api
tokio = {features=['sync']} # state_manager
tokio = {features=['rt-multi-thread']} # statediff
tokio = {features=['fs', 'io-util']} # utils
tokio-util = {features=['compat']} # genesis
tokio-util = {features=['compat']} # rpc
tokio-util = {features=['compat']} # utils
uuid = {version=1.3,features=['v4']} # db
xsalsa20poly1305 = {version=0.9,features=[]} # key_management

[dev-dependencies]
cs_serde_bytes = {version=0.12.2,features=[]} # utils
forest_genesis = {features=['testing']} # libp2p
fvm_shared = {features=['arb']} # json
fvm_shared3 = {features=['arb']} # json
libp2p = {features=['tcp', 'noise', 'yamux', 'request-response', 'tokio'],default_features=False} # libp2p_bitswap
multiaddr = {version=0.17.1,features=[]} # networks
multihash = {features=['std', 'blake2b', 'derive'],default_features=False} # chain
multihash = {features=['identity'],default_features=False} # ipld
multihash = {version=0.16,features=['sha2'],default_features=False} # utils
num-bigint = {features=['quickcheck']} # json
serde = {features=['derive']} # utils
serde_derive = {version=1.0,features=[]} # utils
sha2 = {features=[],default_features=False} # utils
tokio = {features=['rt', 'macros']} # libp2p
tokio = {features=['macros', 'rt', 'fs', 'io-util']} # utils
tokio-util = {features=['compat']} # libp2p


[features]
default = [] # db
default = [] # fil_cns
default = [] # state_manager
default = ['paritydb'] # statediff
insecure_post = [] # fil_cns
instrumented_kernel = ['dep:stdext'] # interpreter
jemalloc = ['dep:tikv-jemallocator'] # cli_shared
jemalloc = ['rocksdb?/jemalloc'] # db
mimalloc = ['dep:mimalloc'] # cli_shared
paritydb = ['forest_db/paritydb'] # cli_shared
paritydb = ['dep:parity-db'] # db
paritydb = ['forest_db/paritydb'] # statediff
rocksdb = ['forest_db/rocksdb'] # cli_shared
rocksdb = ['dep:rocksdb'] # db
rocksdb = ['forest_db/rocksdb'] # statediff
testing = [] # genesis

# unhandled
{'target': {'cfg(unix)': {'dependencies': {'termios': '0.3'}}}} # utils
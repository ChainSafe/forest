github_checks:
  annotations: false

codecov:
  require_ci_to_pass: no

coverage:
  status:
    project:
      default:
        threshold: 1%
        informational: true
    patch:
      default:
        informational: true

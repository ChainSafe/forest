[package]
name = "forest_integration_tests"
version = "0.1.0"
authors = ["ChainSafe Systems <info@chainsafe.io>"]
edition = "2018"
publish = false

[features]
default = []
submodule_tests = ["cmd_lib"]

[dependencies]
cmd_lib = { version = "0.8.5", optional = true }
